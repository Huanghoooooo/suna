repos:
  - repo: https://github.com/Yelp/detect-secrets
    rev: v1.5.0
    hooks:
      - id: detect-secrets
        name: detect secrets (json/jsonc/env/yaml)
        files: \.(json|jsonc|env|ya?ml)$
        args:
          - --exclude-lines
          - (\{env:[A-Z0-9_]+\}|\$\{[A-Z0-9_]+\})
